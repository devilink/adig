import type { ResolutionResult } from '../../resolutionCtx.ts';
export interface PerformanceTracker {
    measureResolve(callback: () => ResolutionResult): ResolutionResult;
    measureCompile(device: GPUDevice): void;
}
export declare class PerformanceTrackerImpl implements PerformanceTracker {
    #private;
    measureResolve(callback: () => ResolutionResult): ResolutionResult;
    measureCompile(device: GPUDevice): void;
}
export declare class NullPerformanceTracker implements PerformanceTracker {
    measureResolve(callback: () => ResolutionResult): ResolutionResult;
    measureCompile(): void;
}
