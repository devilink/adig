import { fromHalfBits, toHalfBits } from "./numeric.js";
export function writeFloat16(output, value) {
    output.writeUint16(toHalfBits(value));
}
export function readFloat16(input) {
    return fromHalfBits(input.readUint16());
}
