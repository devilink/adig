import type * as wgsl from './wgslTypes.ts';
import { type BaseData } from './wgslTypes.ts';
import type { InferPatch } from '../shared/repr.ts';
export interface WriteInstruction {
    data: Uint8Array<ArrayBuffer>;
    gpuOffset: number;
}
/**
 * Converts `{idx, value}[]` sparse arrays into `Record<number, T>` format.
 */
export declare function convertPartialToPatch(schema: wgsl.BaseData, data: unknown): unknown;
export declare function getPatchInstructions<TData extends wgsl.BaseData>(schema: TData, data: unknown, targetBuffer?: ArrayBuffer): WriteInstruction[];
export declare function patchArrayBuffer<T extends BaseData>(buffer: ArrayBuffer, schema: T, data: InferPatch<T>): void;
