import { at as asLocal, dt as mixExpr, ft as vec4, pt as Expr, ut as floatE } from "./lower-yTKQx3pz.js";
var toE = (v) => typeof v === "number" ? floatE(v) : v;
var emit = (v, ctx) => typeof v === "number" ? floatE(v)._emit(ctx) : v._emit(ctx);
function callB(name, ...args) {
	return new Expr((ctx) => `${name}(${args.map((a) => emit(a, ctx)).join(", ")})`);
}
function binary(op, a, b) {
	return new Expr((ctx) => `(${emit(a, ctx)} ${op} ${emit(b, ctx)})`);
}
const add = (a, b) => binary("+", a, b);
const sub = (a, b) => binary("-", a, b);
const mul = (a, b) => binary("*", a, b);
const div = (a, b) => binary("/", a, b);
const neg = (a) => new Expr((ctx) => `(-(${emit(a, ctx)}))`);
const abs = (x) => callB("abs", x);
const floor = (x) => callB("floor", x);
const fract = (x) => callB("fract", x);
const sqrt = (x) => callB("sqrt", x);
const pow = (x, y) => callB("pow", x, y);
const exp = (x) => callB("exp", x);
const sin = (x) => callB("sin", x);
const cos = (x) => callB("cos", x);
const atan2 = (y, x) => callB("atan2", y, x);
const min = (a, b) => callB("min", a, b);
const max = (a, b) => callB("max", a, b);
const clamp = (x, lo, hi) => callB("clamp", x, lo, hi);
const mix = (a, b, t) => mixExpr(toE(a), toE(b), typeof t === "number" ? t : t);
const step = (edge, x) => callB("step", edge, x);
const smoothstep = (e0, e1, x) => callB("smoothstep", e0, e1, x);
const length = (v) => callB("length", v);
const dot = (a, b) => callB("dot", a, b);
const normalize = (v) => callB("normalize", v);
const rotate2 = (p, cosA, sinA) => vec2(sub(mul(member2(p, "x"), cosA), mul(member2(p, "y"), sinA)), add(mul(member2(p, "x"), sinA), mul(member2(p, "y"), cosA)));
var member2 = (v, m) => typeof v === "number" ? v : v.member(m);
const reflect = (i, n) => callB("reflect", i, n);
const refract = (i, n, eta) => callB("refract", i, n, eta);
const exp2 = (x) => callB("exp2", x);
const hashPhase = (seed, salt) => mul(fract(mul(sin(mul(seed, salt.k)), salt.big)), 6.283185307179586);
const le = (a, b) => binary("<=", a, b);
const gt = (a, b) => binary(">", a, b);
const ge = (a, b) => binary(">=", a, b);
const select = (cond, ifTrue, ifFalse) => new Expr((ctx) => `select(${emit(ifFalse, ctx)}, ${emit(ifTrue, ctx)}, ${emit(cond, ctx)})`);
const float = (n) => floatE(n);
const vec2 = (x, y) => callB("vec2f", x, y);
const vec3 = (x, y, z) => callB("vec3f", x, y, z);
const vec4$1 = (...parts) => vec4(...parts.map(toE));
const splat3 = (x) => callB("vec3f", x);
const local = (e, hint) => asLocal(e, hint);
const softPlus = (x, k) => {
	const xe = local(x, "sp");
	return mul(add(xe, sqrt(add(mul(xe, xe), mul(k, k)))), .5);
};
const gaussBell = (x, k) => exp(neg(mul(mul(x, x), k)));
export { select as A, vec4$1 as B, mul as C, reflect as D, pow as E, sqrt as F, step as I, sub as L, smoothstep as M, softPlus as N, refract as O, splat3 as P, vec2 as R, mix as S, normalize as T, le as _, cos as a, max as b, exp as c, floor as d, fract as f, hashPhase as g, gt as h, clamp as i, sin as j, rotate2 as k, exp2 as l, ge as m, add as n, div as o, gaussBell as p, atan2 as r, dot as s, abs as t, float as u, length as v, neg as w, min as x, local as y, vec3 as z };
