export interface ComponentProps {
    colorA: string;
    colorB: string;
    /**
     * Pattern scale (higher = more, finer scratches)
     *
     * Accepts a number between -2 and 5.
     * @default 2
     */
    scale: number;
    /**
     * Thickness of the scratches (higher = bolder streaks)
     *
     * Accepts a number between 0.2 and 5.
     * @default 1
     */
    thickness: number;
    /**
     * Random seed for pattern variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    speed: number;
}
export declare const componentDefinition: import('../..').GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map