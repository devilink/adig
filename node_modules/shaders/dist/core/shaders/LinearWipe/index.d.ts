import { GpuShaderDefinition } from '../../gpu/porters';

export interface ComponentProps {
    /**
     * How far the wipe has travelled (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Direction of the wipe in degrees (0 = left to right)
     *
     * @default 0
     */
    angle: number;
    /**
     * Softness of the wipe edge
     *
     * @default 0.1
     */
    feather: number;
    /**
     * Reverse the direction the wipe travels
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map