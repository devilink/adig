import { GpuShaderDefinition } from '../../gpu/porters';
import { transformAngle } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Defocus amount — how far the lens blur spreads
     *
     * @default 50
     */
    radius: number;
    /**
     * How strongly bright highlights bloom into discs
     *
     * @default 4
     */
    highlightGain: number;
    /**
     * Brightness above which a highlight forms a disc
     *
     * @default 0.6
     */
    highlightThreshold: number;
    /**
     * Aperture shape the highlights bloom into — bladed iris or a novelty cut-out (heart, star, …)
     *
     * Accepts one of: `"blades"`, `"circle"`, `"star"`, `"heart"`, `"flower"`, `"cross"`, `"ring"`.
     * @default "blades"
     */
    bladeShape: string;
    /**
     * Blade or point count for the Blades, Star and Flower shapes — 0–2 blades is a circular iris
     *
     * Accepts a number between 0 and 9.
     * @default 6
     */
    bladeCount: number;
    /**
     * Rotation of the aperture shape (in degrees)
     *
     * @default 0
     */
    bladeRotation: Parameters<typeof transformAngle>[0];
    /**
     * Lens colour fringing on the disc edges
     *
     * @default 0.2
     */
    chromaticFringe: number;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map