import { GpuShaderDefinition } from '../../gpu/porters';

export interface ComponentProps {
    /**
     * How far the dissolve has progressed (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Frequency of the dissolve pattern (higher = smaller blobs)
     *
     * @default 3
     */
    scale: number;
    /**
     * How softly the erosion edge fades out (0 = hard-edged blobs)
     *
     * @default 0.25
     */
    softness: number;
    /**
     * Random seed for pattern variation
     *
     * @default 0
     */
    seed: number;
    /**
     * Reverse the order the pattern dissolves in
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map