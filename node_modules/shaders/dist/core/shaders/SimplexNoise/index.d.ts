import { ColorStop } from '../../utilities/colorStops';

export type { ColorStop };
export interface ComponentProps {
    colorA: string;
    colorB: string;
    stops: ColorStop[] | null;
    colorSpace: string;
    /**
     * Pattern scale (higher = larger patterns)
     *
     * Accepts a number between -2 and 5.
     * @default 2
     */
    scale: number;
    /**
     * Balance between colors (negative = more colorB, positive = more colorA)
     *
     * @default 0
     */
    balance: number;
    /**
     * Pattern contrast (higher = sharper transitions)
     *
     * @default 0
     */
    contrast: number;
    /**
     * Random seed for pattern variation
     *
     * Accepts a number between 0 and 100.
     * @default 0
     */
    seed: number;
    /**
     * Animation speed
     *
     * Accepts a number between 0 and 5.
     * @default 1
     */
    speed: number;
}
export declare const componentDefinition: import('../..').GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map