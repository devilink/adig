import { transformColor } from '../../utilities/transformations';

export interface ComponentProps {
    /**
     * Brush size — radius of the painterly flattening
     *
     * Accepts a number between 1 and 8.
     * @default 3
     */
    radius: number;
    /**
     * Amount the pigment bleeds and wobbles past hard edges
     *
     * @default 1
     */
    bleed: number;
    /**
     * Blend between the original image (0) and the full watercolor effect (1)
     *
     * @default 1
     */
    strength: number;
    /**
     * Strength of the paper grain the pigment settles into
     *
     * @default 0.35
     */
    paper: number;
    /**
     * Tint of the paper grain
     *
     * Accepts hex strings (`#ff0000`), RGB objects (`{ r, g, b }`), or CSS color names (`limegreen`).
     * @default "#fbf7ec"
     */
    paperColor: Parameters<typeof transformColor>[0];
}
export declare const componentDefinition: import('../..').GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map