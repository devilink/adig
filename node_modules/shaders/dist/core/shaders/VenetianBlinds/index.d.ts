import { GpuShaderDefinition } from '../../gpu/porters';

export interface ComponentProps {
    /**
     * How far the blinds have closed (0 = fully visible, 1 = fully wiped away)
     *
     * @default 0.5
     */
    progress: number;
    /**
     * Orientation of the strips in degrees
     *
     * @default 0
     */
    angle: number;
    /**
     * Number of strips across the frame
     *
     * @default 5
     */
    stripCount: number;
    /**
     * Softness of each closing strip edge
     *
     * @default 0.15
     */
    feather: number;
    /**
     * Close each strip from the opposite edge
     *
     * Accepts a boolean value (`true` or `false`).
     * @default false
     */
    invert: boolean;
}
export declare const componentDefinition: GpuShaderDefinition<ComponentProps>;
export default componentDefinition;
//# sourceMappingURL=index.d.ts.map