import { ComponentProps } from '../types';
import { GpuShaderDefinition } from '../gpu/contract';
import { StdDefinition } from './types';

/** Lower a std definition to the engine contract. */
export declare function defineStd<T extends ComponentProps>(definition: StdDefinition<T>): GpuShaderDefinition<T>;
//# sourceMappingURL=lower.d.ts.map