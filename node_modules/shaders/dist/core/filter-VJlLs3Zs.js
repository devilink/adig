function tintToward(color, opts) {
	return {
		kind: "tintToward",
		color,
		amount: opts.amount
	};
}
function displaceBy(field, opts) {
	return {
		kind: "displaceBy",
		field,
		strength: opts.strength,
		chromatic: opts.chromatic,
		edges: opts.edges
	};
}
function gather(config) {
	return {
		kind: "gather",
		...config
	};
}
export { gather as n, tintToward as r, displaceBy as t };
